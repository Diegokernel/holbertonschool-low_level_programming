#include "holberton.h"
#include <stdlib.h>

/**
 * **strtow - Prints an array of string
 * @str: The array to print
 *
 * Return: nothing
 */
char **strtow(char *str)
{
	if (str == NULL)
		return (NULL);
}
